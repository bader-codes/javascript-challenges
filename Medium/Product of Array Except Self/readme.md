# Product of Array Except Self

## 🧩 Problem Description

Given an array of numbers, return a new array such that each element at index `i` is the product of all the numbers in the original array **except the one at `i`**.

**Example:**

```javascript
Input:  [1, 2, 3, 4]
Output: [24, 12, 8, 6]